import $ from 'jquery'
import 'jquery-highlight'

// export function highlight(ids, wordsPositions, className) {
//   ids.forEach(function(id) {
//     $('div_id').addClass('highlight-div highlight-div-' + className);
//   })
// }

// const popoverDefaults = {
//   placement: 'auto top',
//   trigger: 'hover',
//   container: 'body',
// }

// export function popover(el, content, options = {}) {
//   let settings = $.extend({}, popoverDefaults, {
//     content: content,
//   })
//   $.extend(settings, options)
//   $(el).popover(settings)
// }

// export function dangerousHtmlPopover(el, content, options = {}) {
//   let settings = {
//     html: true, // dangerous be careful
//   }
//   $.extend(settings, options)
//   popover(el, content, settings)
// }
